<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
              android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
            android:background="#eaeaea">
    <android.support.v7.widget.Toolbar
        android:id="@+id/geren_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorToolbar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:elevation="4dp">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="@string/ge_ren"
            style="@style/ToolBarTextAppearance"/>
    </android.support.v7.widget.Toolbar>
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <include layout="@layout/ge_ren_item"/>
            <include layout="@layout/ge_ren_item01"/>
            <include layout="@layout/ge_ren_item02"/>
            <include layout="@layout/ge_ren_item03"/>

            <Button
                android:id="@+id/exit_login"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="@string/login_exit"
                android:textColor="@color/colorToolbar"
                android:background="@drawable/select_ge_ren_item"
                android:layout_marginTop="16dp"/>
        </LinearLayout>
    </ScrollView>

</LinearLayout>
